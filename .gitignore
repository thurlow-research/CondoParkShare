# Database
data/*.db
data/*.db-wal
data/*.db-shm

# Dependencies
vendor/
composer.phar

# Environment / secrets
.env
.env.*
!.env.example
!.env.test

# IDE
.idea/
.vscode/
*.swp

# Test artifacts
.phpunit.result.cache

# OS
.DS_Store
Thumbs.db

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
*.sqlite3

# Coverage / mutation testing
.coverage
htmlcov/
.mutmut-cache

# AI oversight protocol
# prompts/ and AGENTS.md are always committed — never ignore them
.ai-local/
.claude/settings.local.json

# Agent temp space — ephemeral inter-agent communication, never committed
.claudetmp/
*.salt

# transient MCP browser artifacts
.playwright-mcp/

# mutmut mutation testing output
mutants/
.mutmut-cache

# Runtime audit-recovery JSONL sink (AUDIT_RECOVERY_LOG)
logs/

# Legacy audit log — superseded by audit/log/ per-entry layout (#888)
audit/oversight-log.jsonl

# Local environment overrides (direnv)
.envrc
.hos-brownfield/
