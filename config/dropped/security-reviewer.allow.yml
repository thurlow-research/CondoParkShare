- line: "You are the security reviewer for CondoParkShare. You run after code-reviewer has approved. Your job is to find vulnerabilities — not code quality issues. Be thorough and adversarial: assume an attacker who is also a registered resident of the building."
  covered_by: "CORE"
  reason: "CORE intro defines the adversarial posture and the authenticated-insider attacker; PROJECT threat model names the registered resident as primary attacker. The role/lane framing (runs after code-reviewer, vulnerabilities not code quality) is in CORE."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "## Threat model for this application"
  covered_by: "PROJECT"
  reason: "PROJECT region has the '### Threat model (drive every review from this)' heading carrying the CPS actor list; this generic heading is superseded by it."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Login form does not reveal whether an email exists (timing attack / enumeration)."
  covered_by: "CORE"
  reason: "CORE Authentication & session: 'Credential checks do not leak account existence (timing/enumeration on login or reset).'"
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Invite tokens and recovery codes are generated with `secrets.token_urlsafe()` or equivalent cryptographic PRNG — not `random`."
  covered_by: "CORE"
  reason: "CORE Authentication & session: 'Tokens and secrets (invite tokens, recovery codes) are generated with a cryptographic PRNG, not a non-cryptographic random source.'"
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "**Authorization — multi-tenant isolation:**"
  covered_by: "PACK:django"
  reason: "Generic Authorization heading; multi-tenant isolation now covered by CORE Authorization plus the django pack's 'Multi-tenant / org-scoped queryset isolation' section."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Every view that loads a model instance first verifies `instance.organization == request.user.organization` — not just an ID check."
  covered_by: "PACK:django"
  reason: "django pack 'Multi-tenant / org-scoped queryset isolation': every get/filter on a tenant-scoped model must be followed by an org/tenant equality check, not an existence check (CWE-639); CORE requires ownership/scope-check not ID-check."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- The operator console is unreachable by non-superusers — including HOA admins of other buildings."
  covered_by: "PROJECT"
  reason: "PROJECT 'Operator console & portal reachability': operator console must be unreachable by any non-superuser including a fully-authenticated HOA admin of another building, verified with the cross-tenant actor."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Tenant-scoped admin views cannot be navigated to with a different tenant's object IDs (IDOR check)."
  covered_by: "PROJECT"
  reason: "PROJECT 'Operator console & portal reachability' scopes the manager portal to one org and forbids navigating to another org's objects by ID (CPS instance of the IDOR rule, incl. admin audit-log views); django pack overrides admin get_queryset on tenant-scoped admin classes."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Template auto-escaping is on; no `|safe` or `mark_safe()` on user-controlled data."
  covered_by: "PACK:django"
  reason: "django pack 'Django template injection and mark_safe': |safe on user-derived data and mark_safe(user_data) are critical XSS findings, and autoescape must not be globally False."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- CSRF middleware is active; `@csrf_exempt` is not used except where provably safe (HTMX endpoints still need CSRF tokens)."
  covered_by: "PACK:django"
  reason: "django pack 'CSRF: middleware coverage and HTMX' requires CsrfViewMiddleware present, flags @csrf_exempt on state-changing views absent documented justification, and requires HTMX state-changing requests to deliver the CSRF token."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "This creates a permanent record even when findings are resolved pre-merge — used by the historical risk assessor to identify persistently risky areas."
  covered_by: "CORE"
  reason: "CORE 'Finding the record' explains the resolved-in-review security-finding issue is filed 'so the historical risk assessor sees persistently risky areas' — same rationale, reworded."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- Do not re-raise issues that were addressed correctly."
  covered_by: "CORE"
  reason: "Generic re-review discipline; CORE output format states 'On re-review, only re-check changed code plus anything that change could affect,' which subsumes not re-raising correctly-addressed findings. Not CPS-specific."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"

- line: "- **Loop exit:** After 5 rounds without full approval, escalate to the architect with: the iteration count, which findings have persisted, and what the coder changed each time. Do not attempt a 6th round."
  covered_by: "CORE"
  reason: "CORE 'Iteration & loop exit' sets the 5-round cap with no 6th round and escalation to architect; the persisted-findings/iteration detail is generic loop discipline preserved in CORE plus the temp-state round record."
  approved_by: "ai-triage (pending human review)"
  approved_at: "2026-06-15"
