"""
parking.admin — parking app admin registrations.

All admin registrations for parking models live in operator_console/admin.py
so the operator console can co-locate them with user/audit registrations
and apply the unscoped-manager pattern uniformly.
"""
